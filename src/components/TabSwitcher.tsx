import {useState} from "react";
import {View} from "react-native";

const TabSwitcher = () => {
  const [activeTab, setActiveTab] = useState<boolean>(false);
  const [indicatorAnim] = useState<boolean>(false);
  
  return (
      <View>
          
          </View>
  )
};
